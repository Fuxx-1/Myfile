#include <stdio.h>
#include <algorithm>
int main(void)
{
    int t;
    scanf("%d", &t);
    int a[10000];
    for (size_t i = 0; i < t; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    
    return 0;
}
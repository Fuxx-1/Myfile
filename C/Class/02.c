/* 
abcd
ac bd
 */
#include <stdio.h>
#include <stdlib.h>
int main(void)
{
    // int x;
    // scanf("%d", &x);
    // int a, b, c, d;
    // a = x / 1000;
    // b = x / 100 % 10;
    // c = x / 10 % 10;
    // d = x % 10;
    // printf("%d ", a * 10 + c);
    // printf("%d", b * 10 + d);

    int r = 4;
    double y = r % 2;
    printf("%d\n", y);
    
    system("pause");
    return 0;
}
#include <stdio.h>

int main(void)
{
    int n, l;
    scanf("%d%d", &n, &l);
    int b[30];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < l; i++)
    {
        if (l / a[i] > )
        {
            /* code */
        }
        
    }
    
    return 0;
}